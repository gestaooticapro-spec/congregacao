## Plano: Adicionar marcação "Território Pessoal" / "Congregação"

### 1. Nova migração SQL
**Arquivo:** `supabase/migrations/20260817_add_territorio_pessoal.sql`
- Adicionar coluna `territorio_pessoal` (boolean, default `false`) na tabela `territorios`
- Grant de SELECT/INSERT/UPDATE para anon, authenticated

### 2. Atualizar tipos TypeScript
**Arquivo:** `types/database.types.ts`
- Adicionar `territorio_pessoal: boolean` em Row, Insert e Update da tabela `territorios`

### 3. Nova server action
**Arquivo:** `app/actions/territorios.actions.ts`
- Adicionar função `toggleTerritorioPessoal(territorioId: string)` que faz update do campo `territorio_pessoal` (toggle true/false)
- Revalidar paths `/territorios` e `/territorios/${id}`

### 4. Página de detalhe do território
**Arquivo:** `app/territorios/[id]/page.tsx`
- Carregar o campo `territorio_pessoal` do território (já vem do `getTerritory` que faz `select('*')`)
- Adicionar um checkbox/toggle na tela de trabalho (após atribuído), abaixo do nome do responsável, com label "Território Pessoal"
- O toggle chama a server action `toggleTerritorioPessoal` com optimistic update
- Também mostrar o badge de categoria no header (vermelho para pessoal, azul para congregação)

### 5. Página de listagem (cards)
**Arquivo:** `app/territorios/page.tsx`
- No select da query, o campo já virá porque é `select('*')`
- No card, substituir a informação do responsável por um badge de categoria:
  - Se `territorio_pessoal` = true → badge vermelho **"Território Pessoal"**
  - Se `territorio_pessoal` = false → badge azul **"Congregação"**
- Manter o nome do responsável abaixo do badge

### Resumo do fluxo:
1. Usuário escolhe território → atribui a si mesmo
2. Na tela de trabalho, marca "Território Pessoal" (opcional, default é Congregação)
3. O card na aba "Reservados" mostra o carimbo visual correspondente